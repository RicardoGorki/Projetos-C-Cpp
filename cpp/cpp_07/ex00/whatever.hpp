#ifndef WHATEVER_HPP
# define WHATEVER_HPP

template < typename T >
T const & swap( T const & x, T const & y)
{
	T const
}

T const & min( T const & x, T const & y)
{

};
T const & max( T const & x, T const & y)
{
};
#endif
